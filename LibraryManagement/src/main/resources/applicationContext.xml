<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/aop
       http://www.springframework.org/schema/aop/spring-aop.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Instructs Spring to auto-discover classes marked with @Service, @Repository, etc. -->
    <context:component-scan base-package="com.library" />

    <!-- Enables AspectJ Auto-Proxying so Spring wraps beans in proxy wrappers -->
    <aop:aspectj-autoproxy />

    <!-- Keeping our AOP Logging configuration from Exercise 3 & 8 intact -->
    <bean id="loggingAspect" class="com.library.aspect.LoggingAspect" />

    <!-- AOP Configuration to trap custom annotated methods -->
    <aop:config>
        <aop:aspect ref="loggingAspect">
            <!-- Intercepts only methods explicitly marked with @TrackExecutionTime -->
            <aop:around method="logExecutionTime" 
                        pointcut="@annotation(com.library.annotation.TrackExecutionTime)" />
        </aop:aspect>
    </aop:config>

</beans>